var ivNet;
(function (ivNet) {

    (function (carosel) {
       
        carosel.initialise = initialise;

        // api
        // root
        function initialise() {
            setup();
            //eventBinding();
        }

        function setup() {
           

        }
    })(ivNet.carosel || (ivNet.carosel = {}));

    var carosel = ivNet.carosel;
})(ivNet || (ivNet = {}));
